# This is an example environment file.
# Copy this file to .env in the same directory (`backend/.env`) to configure your local environment
# for running Prisma commands directly on your host machine.
# NOTE: The DATABASE_URL for the running Docker container is set in the docker-compose.yml file.

# PostgreSQL connection string
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL="postgresql://transituser:transitpassword@localhost:5433/transitdb"